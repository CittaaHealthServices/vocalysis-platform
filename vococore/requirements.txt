flask==3.0.0
gunicorn==21.2.0
librosa==0.10.0
numpy==1.24.3
scipy==1.11.2  # matches training/requirements.txt
soundfile==0.12.1
pydub==0.25.1
praat-parselmouth==0.4.3
python-dotenv==1.0.0
# ── ML artifact dependencies ─────────────────────────────────────────────────
# These MUST match the exact versions used when vocacore_ensemble_v2.joblib
# was serialised. Loading a joblib artifact across different sklearn/xgboost
# major/minor versions risks AttributeErrors, wrong feature names, or worst
# case silently incorrect predictions. See training/requirements.txt.
scikit-learn==1.3.0
xgboost==2.0.3
joblib==1.3.1
pandas==2.0.3
# Background noise removal (critical for Indian real-world environments)
noisereduce==3.0.0
# ElevenLabs voice generation pipeline
elevenlabs==1.0.0
httpx[socks]==0.25.2
# Auto-retraining scheduler
APScheduler==3.10.0
