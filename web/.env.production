# Production environment — baked into the Vite build at deploy time
# Railway reads this during `vite build`

VITE_API_URL=https://vocalysis-platform-production.up.railway.app
VITE_APP_NAME=Vocalysis
VITE_SUPPORT_EMAIL=support@cittaa.in
